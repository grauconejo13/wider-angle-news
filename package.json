{
  "name": "wider-angle-news",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-news:raw": "node scripts/fetch-news.mjs",
    "scout": "node scripts/scout-quality.mjs",
    "fetch-news": "npm run fetch-news:raw && npm run scout",
    "refresh": "npm run fetch-news && npm run build"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
